# ai-convex-clone

Monorepo for a Convex-inspired database system.

## Packages

- `crates/core-db`: Rust core database primitives.
- `packages/sdk`: TypeScript SDK and schema surface.

## Commands

- `bun install`
- `bun run lint`
- `bun run test`
